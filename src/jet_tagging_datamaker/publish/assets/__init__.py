# Files copied verbatim into the record: loader/, configs/ and requirements.txt.
#
# Nothing here is imported by the datamaker. The package marker only keeps the
# directory inside the installed distribution, so that copy_assets finds it beside
# publish/huggingface.py whether the datamaker runs from a checkout or from a wheel.
